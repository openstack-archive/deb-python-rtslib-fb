#!/bin/sh

set -e

if [ "$1" = "configure" ] ; then
	update-alternatives --remove targetctl /usr/bin/targetctl-python3
	update-alternatives --install /usr/bin/targetctl targetctl /usr/bin/python3-targetctl 200
fi

#DEBHELPER#
