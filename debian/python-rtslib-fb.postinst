#!/bin/sh

set -e

if [ "$1" = "configure" ] ; then
	update-alternatives --remove targetctl /usr/bin/targetctl-python2
	update-alternatives --install /usr/bin/targetctl targetctl /usr/bin/python2-targetctl 300
fi

#DEBHELPER#
